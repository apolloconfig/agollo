language: go
sudo: required

go:
  - 1.12

env:
    - GO111MODULE=on

before_install:
    - go get github.com/mattn/goveralls
    - Project=apollo

install:
  - go mod vendor

script:
    - chmod u+x coverage.sh
    - ./coverage.sh --coveralls
    - go build -o "$Project"
